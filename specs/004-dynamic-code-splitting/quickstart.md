# Quickstart: Validating Dynamic Code-Splitting

## Build Command
```bash
export PATH=/Users/igorgoc/.nvm/versions/node/v20.20.2/bin:$PATH
cd frontend
npm run build
```

*Expected Output*: Multiple `.js` chunks emitted in `dist/assets/`, including separate files for `ConfigTab`, `MaintenanceTab`, `LogsTab`, `StorageTab`, `ValidatorTab`, `NetworkTab`.
